# Python environment
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Secrets
.env
.env.*
!.env.example

# Private project working files (owner/mentor only, not part of the public repo)
.ai/
CLAUDE.md
AGENTS.md
archive/

# Notebook checkpoints / OS cruft
.ipynb_checkpoints/
.DS_Store

# Data — the source dataset (UCI Online Retail II, id 502, CC BY 4.0, 43.5 MB)
# is too large to commit; `make data` downloads it to data/raw/ and writes the
# cleaned data to data/processed/. Both are gitignored. Only the small,
# deterministic fixture sample under data/fixtures/ (<1 MB, see data/README.md)
# is committed, for tests.
data/raw/
data/processed/

# Pipeline run artifacts.
outputs/
